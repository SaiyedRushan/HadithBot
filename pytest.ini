[pytest]
# Only collect the real unit tests under tests/. The root-level test_setup.py /
# test_hadith_progression.py are interactive smoke scripts that hit the live DB,
# not pytest tests, so they are intentionally excluded.
testpaths = tests
# Put the project root on sys.path so tests can `import utils`, `import db`, etc.
pythonpath = .
